import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: { main: "#FF6B2B" },
    secondary: { main: "#1B2E4B" },
  },
  typography: {
    fontFamily: "Inter, system-ui, sans-serif",
  },
});
